# Shipped — 2026-06-28 W3 (D-099–D-102)

RAG quality (eval-harness-first) + V1-blocker fix + runbook + final-health. Autonomous,
Opus 4.8. **Zero migrations** — all changes were code (a new script), prod data, or docs;
ledger unchanged at 82 / `6355f130c92519af0bc106d9938939ae`. All pushed to `main`
(`5e7cca8` → `f262fe9`).

## The throughline: measure-first beat ship-the-assumed-fix

W3's premise (from the handover + D-070 + code comments) was that RAG quality is gated by
**priority-1 crowding**, fixable by raising `RERANK_INPUT_LIMIT`. Building the eval harness
*first* let measurement override that — and it did, three times. We shipped a durable quality
gate and a true picture instead of a no-op tuning change before a CEO/CFO demo.

## What shipped

| D | What | Commit |
|---|---|---|
| **D-099** | **RAG eval harness** `scripts/rag-eval.ts` — replays 84 gold questions through live `rag-query`, LLM-judges vs the 2026-06-22 reference (direction-aware), self-cleans prod sessions. Baseline **77.4% strict** (not the cited 92.9% — that was a one-day human pass). Crowding/rerank-limit theory **refuted by telemetry**. | `5e7cca8` |
| **D-100** | Measured the approved fixes: **F1** demote priority-1 (31→12) = neutral (76.2%) → **reverted**; **F4** embed 3 NULL context rows = kept (D-070 backfill); **F2** refusal-tuning = **not shipped** (wrong lever + would risk hallucinating around purged secrets). **Discovery:** ~5 "regressions" are correct refusals of denylisted secret data → **genuine quality ≈ 84%**. | `e683699` |
| **V1 fix** | Ran the Stage-8 key-user seed on **prod** (accounts only, no emails): profiles **4→10** (4 super_admin / 5 admin / 1 user). The demo's role-gated views now work. ⚠️ Emirkan = `user` (Buhara's call); 6 temp passwords to reset. | `069f24e` |
| **D-101** | **`spec/RUNBOOK.md`** — the operational/demo-day incident playbook that didn't exist (recon: 28 spec docs, 0 runbooks). Pre-demo checklist, symptom→fix, rollback procedures, known-good baseline. | `7763855` |
| **D-102** | Final-health: **🟢 GO, zero blockers** (`spec/HEALTH_CHECK_2026-06-28.md`). Live re-snapshot + ledger parity re-verified (repo↔registry exact) + derived-doc reconcile (ARCHITECTURE §16→D-102 & functions 167→163, §1 22→34 tables, §4 55→51 pages, README D-095→D-102). | `f262fe9` |

## Working-agreement notes (this batch)
- **Premise-first recon overrode the plan** (lesson #1): refuted rerank-limit, F1, and F2 with telemetry/measurement before shipping any of them.
- **Lightweight technique** (lesson #2): offline rerank simulation + reading the harness's own captured answers instead of re-running prod, to separate retrieval-vs-prompt-vs-security causes.
- **Branch hygiene** (lesson #7): the working dir was switched to `feat/duty-free` (parallel work) mid-session *after* the start check, so D-099 first landed on the wrong branch — caught it, cherry-picked to `main`, restored `feat/duty-free`. New rule: re-verify `git branch` before **every** commit.
- **Measure before enshrine:** applied F1 to prod, measured (neutral), reverted — committed the migration/decision only for what the harness confirmed.

## Open (owner action / post-demo)
- Reset the 6 seeded temp passwords; decide Emirkan's role (`user`→super_admin?); email-template swap (D-071); E2E CI repo secrets (`TEST_USER_*`).
- RAG genuine-quality levers (each harness-measurable): denylist-aware harness → F3 retrieval granularity → validated content corrections (Pegasus PNR, Y360, Mavi Gök — needs fact sign-off). F2 refusal-tuning remains an explicit product decision.
