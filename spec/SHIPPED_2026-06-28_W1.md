# W1 Demo-Prep Sprint — Shipped 2026-06-28 (D-091–D-094)

**Mode:** autonomous (Opus 4.8), following the D-086–090 batch. Zero migrations (as planned) —
ledger unchanged at **82**, hash `6355f130c92519af0bc106d9938939ae` (verified both sides).

| D | Task | Status | Key result |
|---|---|---|---|
| D-091 | Repo drift cleanup | ✅ fixed | Reverted an inert React Compiler WIP; fixed pnpm `sharp: false` → `pnpm install` exit 0, tree clean |
| D-092 | Cron warm-up verify | ✅ 🟢 | `net._http_response`: pings reach rag-query (400) every 4 min; warm path TTFB 2.9s vs ~7.9s cold |
| D-093 | Signed-URL optimization | ✅ analyzed (no change) | The 2 Supabase calls are dependent → doc's options don't apply; route near-optimal, latency acceptable |
| D-094 | Folder-tree authed probe | ✅ 🟡 | TTFB p50 0.67s / p95 0.90s; **DB is 0.1ms** — cost is auth-SSR overhead, not folder logic |

## What each really found

- **D-091** — The "uncommitted drift" was two unrelated things: an **inert, incomplete React
  Compiler experiment** (babel.config.js, which Next-16 Turbopack ignores; no `next.config`
  wiring; nothing imports it) — **reverted**; and pnpm 11 demanding a build-script decision for
  `sharp`, writing a placeholder into `pnpm-workspace.yaml` and exiting 1 — **the real**
  "pnpm install fails", fixed with `allowBuilds: sharp: false`. Verified: install/tsc/build all
  green. *If React Compiler is wanted, add it via `next.config experimental.reactCompiler`, not
  a babel config — flagged.*
- **D-092** — Verified from telemetry (cron run history + pg_net response log) instead of the
  disruptive disable-cold-test, which would have risked leaving the demo function cold. The pings
  provably reach rag-query every 4 min → it never goes cold. 🟢
- **D-093** — Recon showed the row-read and the signed-URL mint are **sequential and dependent**,
  so the prompt's parallelize/SQL-function options are infeasible and edge runtime only trims
  cold-start. The ~0.48s also includes client RTT; a file opens sub-second. **No risky change to
  demo-critical serving was justified.** Post-demo lever: edge-cached signing (arch change).
- **D-094** — Measured the login-gated folder-tree via a **constructed `@supabase/ssr` cookie**
  (no Playwright, no tree-dirtying). EXPLAIN proved the folder query is 0.1ms; the ~0.67s TTFB is
  generic authed-SSR + `getUser()` JWT-validation overhead shared by all authed pages.

## Demo-readiness

- 🟢 RAG cron warm-up verified keeping the function warm.
- 🟢 Repo tree clean; `pnpm install` works again.
- 🟡 Signed-URL serving (~0.48s) and folder-tree TTFB (~0.67s) are over aspirational targets but
  acceptable for an internal tool; both bottlenecks are network/auth-SSR overhead, **not** DB or
  app logic (DB queries are sub-millisecond).
- ⏸ Auth DB connections → percentage was already done via the Management API (prior batch).

## Remaining open (post-demo)

- `pg_net` schema lint (cosmetic), the 3 anon-kept SECDEF helpers (product decision) — from prior batch.
- Authed-SSR latency lever = the per-request `getUser()` validation (affects all authed routes) — a
  considered security/latency tradeoff.
- React Compiler — adopt deliberately via `next.config` if wanted.

Branch `main`, ledger **82/82**, hash `6355f130…` (unchanged). 4 task commits + this summary.
