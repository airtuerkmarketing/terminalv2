name: E2E Smoke Tests

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm e2e
        env:
          # Buhara TODO: set these as repo secrets / variables.
          E2E_BASE_URL: ${{ vars.PROD_URL || 'https://www.airtuerk.dev' }}
          TEST_USER_EMAIL: ${{ secrets.bdemir@airtuerk.de }}
          TEST_USER_PASSWORD: ${{ secrets.Aa1!768ff7da77aba93dd42e4214a057f36d }}
      - uses: actions/upload-artifact@v4
        if: ${{ failure() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
